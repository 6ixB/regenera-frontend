export enum FrontendRoutesEnum {
  SIGNIN = "/auth/signin",
  SIGNUP = "/auth/signup",
  HOME = "/",
  ABOUT = "/about",
  DASHBOARD = "/dashboard",
}

export enum BackendRoutesEnum {
  AUTH = "/auth",
  USERS = "/users",
}
